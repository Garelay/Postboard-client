import React from 'react'

export default function ModalError() {
    return (
        <div>
            There seems to be an error, click here to return back to the site.
            <button>Get Back</button>
        </div>
    )
}
